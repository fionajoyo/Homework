package com.Internet;

import java.io.*;
import java.net.Inet4Address;
import java.net.InetAddress;
import java.net.Socket;

/**
 * @Author=fionajoyo
 * @Date 2021/7/8 14:52
 * @Version 1.0
 */
public class test {
    public static void main(String[] args) throws IOException {
        /*Mail m=new Mail("New york","Beijing","Hello");
        ObjectOutputStream oops=new ObjectOutputStream(new FileOutputStream("Mail.ser"));
        oops.writeObject(m);
        oops.flush();
        oops.close();
        System.out.println("saved in Mail.ser");*/
        /*Mail m;
        ObjectInputStream oips=new ObjectInputStream(new FileInputStream("Mail.ser"));
        try {
            m=(Mail) oips.readObject();
            m.mail();
        } catch (ClassNotFoundException e) {
            e.printStackTrace();
        }
        oips.close();*/




    }
}
